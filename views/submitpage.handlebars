<div class="left-col">
	<a href="/">
		<div class="logo">
			Sample Code
		</div>
	</a>
	<div class="list-title">
		Frameworks
	</div>
	<div class="col-item-list">
		{{#each frameworks}}
			<a href="/framework/{{name}}">
				<div class="row-item">{{name}}</div>
			</a>	
		{{/each}}
	</div>
	<a href="/submit">
		<div class="col-item submit-sample">
			Submit Sample
		</div>
	</a>
</div>

<div class="content-col" ng-controller="SubmitController">
	
	<div class="submit-title">
		<strong>Submit a SampleCode</strong>
	</div>
	
	<form name="form" ng-submit="submit(sample)" class="submit-form">
		
		<p class="input-info">Embeded Demo Code</p>
		
		<textarea rows="7" ng-model="sample.embeded" required></textarea>
		
		<p class="input-info">Description</p>
		
		<textarea rows="7" ng-model="sample.description" required></textarea>
		
		<div class="center-form">
			
			<div class="input-row">
				<span>Framework/Language:</span>
				<span class="dropdown">
					<select type="text" 
						ng-model="sample.framework" 
						ng-options="framework.name for framework in frameworks track by framework.value || framework.name" 
						required="">
					</select>
				</span>
			</div>
			
			<div ng-if="sample.framework.value == 'new'" class="input-row">
				<span class="new-item">New Framework/Langauge:</span>
				<input type="text" ng-model="sample.newFramework" required=""/> *
			</div>
			
			<div class="input-row">
				<span>Title for sample:</span>
				<input type="text" ng-model="sample.title" required=""/> *
			</div>
			
			<div class="input-row">
				<span>Your Name/Nickname:</span>
				<input type="text" ng-model="sample.name" required=""/> *
			</div>
			
			<div class="input-row">
				<span> Email:</span>
				<input ng-model="sample.email" type="text" />
			</div>
			
			<div class="input-row">
				<span class="wide-span">
					* required <br/>
					<br/>
					email is used to stay in contact with you about
					your sample if needed, nothing else
				</span>
			</div>
			
		</div>
		
		<div class="button-row">
			<span class="message" ng-if="submittedCorrect">Post submitted! Will approve samplecode shortly</span>
			<span class="message error-message" ng-if="existingTitle">Post could not be submitted! Title already exists. Pick a new one and try again.</span>
			<button type="submit">Submit</button>
		</div>
		
	</form>
	
</div>